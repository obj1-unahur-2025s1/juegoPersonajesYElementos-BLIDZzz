import miCodigo.*

test "el castillo mide 20 metros" {
  assert.that(castillo.altura() == 20)
}

test "el árbol puede cambiar de altura" {
  tipa.altura(10)
  assert.equals(10, tipa.altura())
}

test "El árbol puede cambiar de altura" {
  tipa.altura(10)
  assert.that(tipa.altura() == 10)
}